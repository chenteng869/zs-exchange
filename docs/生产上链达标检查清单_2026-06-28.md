# 生产上链达标检查清单（可打勾版）

更新时间：2026-06-28
适用项目：Stock Exchange dapp
当前结论：代码质量门已通过；生产运行门未通过。

## 今日执行进展（2026-06-28）

- [x] 已启动依赖容器：Postgres、Redis（healthy）
- [x] 已执行并通过 health:routes
- [x] 已执行并通过 health:p0
- [x] 连续健康探测 3 次全部通过（重试采样）
  - 结果：200 / 200 / 200
- [x] 已输出签名改造设计文档
  - 参考：docs/10-DID身份/DID生产签名改造设计_2026-06-28.md
- [x] 已完成 Prometheus 规则启用改造（代码层）
  - 修改：deploy/prometheus/prometheus.yml
  - 修改：docker-compose.yml
- [ ] Prometheus 运行态验证完成
  - 阻塞：拉取 prom/prometheus:v2.45.0 失败（EOF），属于当前网络/镜像源环境问题
  - 已执行：Docker daemon 镜像源调整并重启 Docker Desktop 进程
  - 当前结果：EOF 仍存在，运行态验收未完成
  - 新阻塞：本机 Docker daemon 出现命令级卡死（docker ps/version 无返回），需先恢复 Docker Desktop 运行时
- [x] 已完成 DID 锚定接口去私钥必填（兼容旧请求）
  - 修改：src/app/api/v1/did/solana/anchor/route.ts
  - 结果：新增 requestId/idempotencyKey 入参；生产环境拒绝 privateKey 输入
- [x] 已完成签名抽象骨架
  - 新增：src/lib/signer/solana-signer.ts
  - 新增：src/lib/signer/index.ts
  - 结果：支持 local/kms provider 选择，kms 为待接入占位实现
- [x] 已完成 KMS provider 接入占位（HTTP/KMS SDK 适配层）
  - 修改：src/lib/signer/solana-signer.ts
  - 修改：.env.example
  - 结果：新增 KMS 端点/鉴权/超时变量，并完成错误码映射
- [x] 已完成 KMS mock 本地联调模式
  - 新增：scripts/mock-kms-server.cjs
  - 新增：scripts/kms-mock-e2e.ts
  - 新增：docs/10-DID身份/KMS_MOCK联调指南_2026-06-28.md
  - 结果：e2e 已通过（keyRef -> KMS mock -> signer -> DID simulate 锚定）
- [x] 已完成 Prometheus 镜像地址参数化
  - 修改：docker-compose.yml
  - 结果：支持 PROMETHEUS_IMAGE 环境变量覆盖
- [x] 已完成主备 RPC fallback 与重试退避
  - 修改：src/modules/did-identity/core/methods/did-sol.service.ts
  - 结果：多 RPC 轮转、指数退避重试、锚定链路容错增强
- [x] 改造后健康门复核通过
  - type-check:p0 通过
  - health:routes 通过
  - health:p0 通过

---

## A. 代码质量门（已通过）

- [x] 全量类型检查通过（mainline）
  - 证据：package 脚本含 type-check:full
  - 参考：docs/health-check-2026-06-26.md
- [x] P0 类型检查通过
  - 证据：package 脚本含 type-check:p0
  - 参考：docs/health-check-2026-06-26.md
- [x] P0 测试通过
  - 证据：package 脚本含 test:p0
  - 参考：docs/health-check-2026-06-26.md
- [x] 路由结构健康检查通过
  - 证据：package 脚本含 health:routes
  - 参考：scripts/route-health-check.cjs

验收命令（重跑）：
- npm.cmd run type-check:full
- npm.cmd run type-check:p0
- npm.cmd run test:p0
- npm.cmd run health:routes

---

## B. 运行环境门（当前阻塞）

- [ ] API 健康检查在目标环境稳定通过（连续 3 次）
  - 验收标准：/api/health 返回 200，且无间歇失败
  - 证据路径：health 输出日志（建议保存在 output/health/）
- [ ] P0 真实链路健康检查在目标环境通过
  - 验收标准：health:p0 全链路成功，订单撮合/余额冻结释放/充值幂等全部断言通过
  - 参考：scripts/health-p0-check.cjs
- [ ] 数据依赖连通通过
  - 验收标准：Postgres/Redis 可连通，应用容器健康
  - 参考：docker-compose.yml
- [ ] 环境变量完整性校验通过
  - 验收标准：DB、Redis、JWT、链上 RPC、签名服务变量均已设置且无默认弱值

验收命令：
- npm.cmd run health:routes
- npm.cmd run health:p0
- docker compose ps
- docker compose logs web --tail=200

---

## C. DID 上链与签名门（当前阻塞）

- [ ] 禁止明文私钥在仓库脚本中出现
  - 当前风险：scripts/did-anchor-real.ts 含硬编码私钥
  - 达标标准：改为 KMS/HSM 或托管签名，不在代码和 .env 明文持有生产私钥
- [ ] DID 上链环境从 devnet 切换到生产网络策略
  - 当前状态：scripts/did-onchain.ts 与 scripts/did-anchor-real.ts 使用 devnet
  - 达标标准：生产网络 RPC、确认策略、失败重试策略就绪
- [ ] 签名审计链路可追踪
  - 达标标准：每次签名具备 request_id、did、tx_hash、operator、结果状态
- [ ] 上链失败补偿与幂等
  - 达标标准：重复提交不重复上链，失败可重放，状态一致

验收建议：
- 先在预生产完成 20 次 DID 上链回归（成功率 >= 99%，无重复锚定）
- 再执行生产小流量（1%）上线

---

## D. 主网连通与资金门（当前阻塞）

- [ ] 主网 RPC 主备可切换
  - 达标标准：主 RPC 故障时，30 秒内切换备 RPC，交易不中断
- [ ] 资金阈值与补给机制
  - 达标标准：Gas 余额低于阈值告警；自动或人工补给流程明确
- [ ] 手续费与确认时间基线
  - 达标标准：定义并监控 p50/p95 确认耗时
- [ ] 回滚与降级策略
  - 达标标准：链上失败时，业务状态回退或进入可恢复中间态

---

## E. 监控告警门（部分具备，需打通）

- [x] 监控基础组件存在
  - 证据：docker-compose.yml 含 Prometheus/Grafana
- [x] 告警规则文件存在
  - 证据：deploy/prometheus/rules/alerts.yml
- [ ] 规则已在 Prometheus 启用
  - 当前状态：代码配置已启用 rule_files，并挂载 rules 目录；运行态验证被镜像拉取阻塞
  - 达标标准：告警规则加载成功，Prometheus UI 可见生效规则
- [ ] 告警通知链路联通（飞书/邮件/Pager）
  - 达标标准：P0、P1 告警可在 1 分钟内送达
- [ ] 链上核心指标入图
  - 达标标准：上链成功率、确认时延、签名失败率、Gas 余额趋势可视化

---

## F. 发布回滚门（待执行）

- [ ] 灰度发布分阶段执行
  - 建议阶段：1% -> 10% -> 25% -> 50% -> 100%
- [ ] 每阶段有明确停止条件
  - 示例：5xx > 0.5%，或上链失败率 > 1% 立即暂停
- [ ] 一键回滚可用
  - 达标标准：5 分钟内回到上一个稳定版本
- [ ] 值班与升级路径明确
  - 达标标准：明确 oncall、升级联系人、应急群

---

## G. 一票否决项（任一未满足不得主网上线）

- [ ] 仍使用明文私钥或本地文件私钥进行生产签名
- [ ] 未完成目标环境 health:p0 验证
- [ ] 未配置主备 RPC 与故障切换演练
- [ ] 无告警通知通道或告警不可达
- [ ] 无回滚演练记录

---

## 单日冲刺执行顺序（小时级）

目标：在 1 个工作日内，把状态从“代码质量达标”推进到“具备预生产放量条件”。

### 09:00-10:00 运行环境解阻

- 任务
  - 固化运行环境（Node、数据库、Redis、端口）
  - 拉起服务并确认 /api/health
- 执行
  - npm.cmd ci
  - docker compose up -d postgres redis
  - npm.cmd run dev
- 产出
  - 环境基线记录（版本、端口、依赖）
  - 首次健康检查截图/日志

### 10:00-11:00 P0 健康链路复核

- 任务
  - 跑通 route 与 p0 health
- 执行
  - npm.cmd run health:routes
  - npm.cmd run health:p0
- 通过标准
  - health:p0 关键断言全部通过（订单、深度、充值幂等、划转）
- 产出
  - health 结果日志（建议存 output/health/health-p0-*.log）

### 11:00-12:00 DID/签名整改设计评审

- 任务
  - 从脚本签名迁移到 KMS/托管签名方案
  - 清理明文私钥风险
- 重点
  - scripts/did-anchor-real.ts 改造为外部签名器接口
  - 明确签名请求协议：request_id、payload_hash、signature、kid
- 产出
  - 签名服务接口草案（含重试和幂等）

### 13:00-14:30 实施签名抽象层

- 任务
  - 新增 signer 适配层（local/dev 与 kms/prod 双实现）
  - DID 上链流程统一走 signer 接口
- 通过标准
  - 代码中不再直接读取生产私钥
  - 单元测试覆盖签名成功/失败/超时
- 产出
  - PR 1：签名抽象层 + DID 调用改造

### 14:30-15:30 主备 RPC 与重试策略

- 任务
  - 增加 RPC fallback（primary/secondary）
  - 增加超时、退避、熔断策略
- 通过标准
  - 模拟主 RPC 故障时可自动切换
- 产出
  - PR 2：RPC 管理器 + 故障演练记录

### 15:30-16:30 监控告警闭环

- 任务
  - 启用 Prometheus 规则加载
  - 增加链上关键指标（上链成功率、确认时延、签名失败率、Gas 余额）
- 执行提示
  - 解除 prometheus.yml 中 rule_files 注释并指向 rules/alerts.yml
- 通过标准
  - Prometheus 可见规则生效
  - 手工触发 1 条告警并收到通知
- 产出
  - PR 3：监控指标与告警链路

### 16:30-17:30 预生产演练与放量决策

- 任务
  - 连续执行 20 次 DID 上链与 API 关键链路
  - 形成 go/no-go 评审结论
- 通过标准
  - 上链成功率 >= 99%
  - P0 API 健康检查 3 连续通过
  - 无 P0/P1 未处置告警
- 产出
  - Go/No-Go 决策单

### 17:30-18:00 回滚演练

- 任务
  - 发布后异常触发回滚
  - 验证 5 分钟内恢复
- 通过标准
  - 回滚耗时、操作步骤、责任人记录完整
- 产出
  - 回滚演练记录与值班手册

---

## 交付判定（今日结束前）

- 通过：A 全绿，B/C/D/E/F 中 P0 必选项全绿
- 暂缓：任一一票否决项未清零
