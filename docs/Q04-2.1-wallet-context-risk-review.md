# Q04-2.1 wallet 上下文风险审查（只读）

> **本报告为 Q04-2.4 recovery 补录，依据当前对话记录、git log、git show、git status 重新生成。**

## 1. 标题

Q04-2.1 wallet 上下文风险审查（只读）：mock / testnet / mainnet / PRIVATE_KEY 上下文重点审查

## 2. 基线

- 审查时间：2026-07-15 02:00 ~ 03:10 (UTC+8)
- 审查模式：**只读**，不修改任何文件，不提交
- HEAD：`51840fec3ebab596276942359cbe3c91ec363e9a`
- origin/main：`1c2a1c38c6062ef40aced63a87e462e7af0fd4b1`
- ahead / behind：4 / 0
- wallet dirty：31 个文件（20M + 11U），diff 大小约 2.7 MB

## 3. 提交 hash

N/A（本报告为只读审查，无 commit）

## 4. commit message

N/A

## 5. 文件范围

### 审查范围

- `src/lib/wallet`（核心服务）
- `src/app/api/v1/wallet`（16 个 route.ts，24 个 method）

### 关键审查文件

- `src/lib/wallet/key/key.service.ts`（48+/154-）
- `src/lib/wallet/chain-registry.ts`（58+/8-）
- `src/lib/wallet/rpc-client.ts`（120+/3-）
- `src/lib/wallet/deposit-credit-service.ts`（47+/17-）
- `src/app/api/v1/wallet/withdrawals/route.ts`（21+/1-）
- `src/app/api/v1/wallet/address-book/route.ts`（7+/25-）

## 6. 关键风险验证

### 6.1 PRIVATE_KEY 分类（A-E）

| 类别 | 数量 | 说明 |
|------|------|------|
| A. process.env | 3 | 全部环境变量注入 |
| B. field/param | 31 | 字段/参数命名 |
| B. param/arg | 195 | 函数参数命名 |
| C. schema/parse | 2 | 解析/校验逻辑 |
| D. comment | 1 | 注释提及 |
| **E. HARDCODED** | **0** | **零硬编码私钥** |

### 6.2 mock 触发路径

- 命中 12 处 mock 触发
- 全部为 `useMockData` / `useMockBroadcast` / `useMockConfirmation` 等配置开关
- 默认值在测试文件触发，**无生产 caller**
- `chain-service.ts:107` / `tron-adapter.ts:232` 的 `fallbackToDemo: true` 需生产 caller 显式 false

### 6.3 testnet / mainnet 触发

- testnet 12 处全部为链配置枚举类型
- mainnet 62 处全部为类型/字段定义
- **0 处硬编码环境选择**

### 6.4 localhost 触发

- 6 处命中，全部为：
  - test fixture（`__tests__/chains/solana-adapter.test.ts`）
  - adapter 字段占位（`bitcoin-adapter.ts:167-168`）
  - mock provider（`broadcast.stage.ts:110`）
- **0 处生产环境 fallback**

### 6.5 secret 触发

- 71 处命中，全部为 `process.env` 引用
- **0 处硬编码 secret**

### 6.6 Wallet API 鉴权扫描

- 24 个 method 全部使用 `requireAuth` 或 `withAdminAuth`
- 全部 16 个 route.ts 实现 try-catch 错误处理
- 鉴权率：**100%**

## 7. ahead / dirty 变化

N/A（只读审查）

## 8. 使用 --no-verify 的原因

N/A（无 commit）

## 9. 后续状态

- ✅ 审查通过，无阻塞风险
- ✅ 进入 Q04-2.2 wallet 域 6 sub-commits 提交阶段
- 🟡 低风险标注：
  - `fallbackToDemo` 默认 true，生产 caller 必须显式传 false
  - `useMock*` 默认 true，当前无生产 caller
  - 3 个新 U 文件待 E2E（webhook-router / webhook-types / withdrawal-resender）
